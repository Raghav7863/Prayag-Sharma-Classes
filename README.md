# Prayag Sharma Classes — Website

## Setup (requires Node.js 18+)

```bash
# 1. Install dependencies
npm install

# 2. Run development server
npm run dev

# 3. Build for production
npm run build
```

Then open http://localhost:5173 in your browser.
